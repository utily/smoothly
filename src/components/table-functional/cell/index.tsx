import { FunctionalComponent, h } from "@stencil/core"

export const SmoothlyTableFunctionalCell: FunctionalComponent = (props, children) => {
	return <div class="smoothly-table-functional-cell">{children}</div>
}
